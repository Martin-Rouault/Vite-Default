export default function Header() {
    return (
        <header>
            <img src="/logo.webp" alt="" />
            <nav>
                <a href="#">Home</a>
            </nav>
        </header>
    );
}
