export default function Header() {
    return (
        <header>
            <nav>
                <img src="/logo.webp" alt="" />
                <a href="#">Home</a>
            </nav>
        </header>
    );
}
